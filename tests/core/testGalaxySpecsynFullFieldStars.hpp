/**
 * @file testGalaxySpecsynFullFieldStars.hpp
 * @author Mark Krumholz
 * @brief Full end-to-end test of a partially-stochastic (field-star) galaxy simulation.
 * @date 2026-08-16
 */

#ifndef TESTGALAXYSPECSYNFULLFIELDSTARS_HPP
#define TESTGALAXYSPECSYNFULLFIELDSTARS_HPP

/**
 * @brief Full end-to-end test of a partially-stochastic (field-star) galaxy simulation.
 * @return 0 if the run passes (including if it was skipped because
 *   the required data files are not present), 1 if it fails -- see
 *   the .cpp file's own comment for what this exercises.
 */
auto testGalaxySpecsynFullFieldStars() -> int;

#endif // TESTGALAXYSPECSYNFULLFIELDSTARS_HPP
