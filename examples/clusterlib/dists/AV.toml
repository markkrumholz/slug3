# Visual extinction (A_V) distribution for the clusterlib example: a
# single powerlaw segment with slope 0 (i.e. flat/uniform) from 0 to
# 3 mag.

breakpoints = [0, 3]

[segment1]
type = "powerlaw"
slope = 0
