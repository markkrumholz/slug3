This example demonstrates nucleosynthetic yield output
(cluster_yields/galaxy_yields) from a small galaxy-type simulation --
MIST tracks, blackbody spectra, and two real yield channels (ccsn and
massive_star_winds, both from the sukhbold16 model), decomposed by
channel. Run this from within this directory with:

slug yieldslib.toml
